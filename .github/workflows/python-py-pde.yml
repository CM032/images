name: Publish Docker image based on python-py-pde

on:
  push:
    branches: [main]
    paths:
      - docker/python-py-pde.Dockerfile
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 2 1 * *"

jobs:
  push_image:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write

    steps:
      - name: Maximize build space
        uses: justinthelaw/maximize-github-runner-space@main
        with:
          cleanup-profile: max
          skip-components: docker-engine

      - name: Partial Clone
        shell: bash
        run: |
          REPO="https://${GITHUB_ACTOR}:${{ secrets.GITHUB_TOKEN }}@github.com/${GITHUB_REPOSITORY}.git"
          git config --global user.email github-actions@github.com
          git config --global user.name github-actions
          git clone -q --filter=blob:none --no-checkout --depth=1 --sparse $REPO && cd images
          git sparse-checkout set docker
          git checkout

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Cache Docker layers
        uses: actions/cache@v5
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-python-py-pde-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-python-py-pde-

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./images/docker/python-py-pde.Dockerfile
          platforms: linux/amd64
          push: true
          tags: ghcr.io/cm032/images/python-py-pde:latest
          cache-from: type=local,src=/tmp/.buildx-python-py-pde-cache
          cache-to: type=local,mode=max,dest=/tmp/.buildx-main-cache

      - name: Move cache
        run: |
          rm -rf /tmp/.buildx-python-py-pde-cache
          mv /tmp/.buildx-main-cache /tmp/.buildx-python-py-pde-cache

      - name: Scan for vulnerabilities
        uses: crazy-max/ghaction-container-scan@master
        with:
          image: ghcr.io/cm032/images/python-py-pde:latest
          annotations: true
          severity_threshold: CRITICAL
          dockerfile: ./docker/python-py-pde.Dockerfile

      - name: Prune untagged images
        uses: carlosal1015/ghcr-delete-image-action@main
        with:
          owner: cm032
          name: images/python-py-pde
          token: ${{ secrets.PAT }}
          untagged-keep-latest: 2
